{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "RAILPACK",
    "buildCommand": "chmod +x gradlew && ./gradlew clean test bootJar"
  },
  "deploy": {
    "startCommand": "/bin/sh -c 'JAR=$(find build/libs -maxdepth 1 -type f -name \"*.jar\" ! -name \"*-plain.jar\" | head -n 1); echo \"Starting $JAR\"; exec java -Dserver.port=$PORT -jar \"$JAR\"'"
  }
}
